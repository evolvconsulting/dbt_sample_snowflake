version: 2

models:
  - name: ff_dbt_log_json
    description: "A table containing a direct copy of the dbt run result files."
    docs:
      show: true
      node_color: "#747474" # Use name (such as node_color: purple) or hex code with quotes (such as node_color: "#cd7f32")
    